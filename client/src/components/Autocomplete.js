import React, { useState, useEffect } from "react";

// User should be able to:
// - Type into search box and have a list of options appear
// - Change the active option with the up and down arrow keys
// - Select the option with a mouse-click or by pressing return/enter

const Autocomplete = ({ options }) => {
  return <div></div>;
};

export default Autocomplete;
